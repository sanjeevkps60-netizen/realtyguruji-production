# ============================================================
# Realty Guruji — Environment Variables
# Copy to .env.local for local dev, and add the same keys in
# Vercel → Project → Settings → Environment Variables.
# The site runs WITHOUT these (curated seed data + guided chat);
# adding them turns on the live database, admin and AI features.
# ============================================================

# Supabase (Project Settings → API)
NEXT_PUBLIC_SUPABASE_URL=https://fnbfexfynotpumkuwnes.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_1fpv133eSKc_AMki-omyQA_ncOBPfCY

# Public site URL (used for SEO/canonical/sitemap)
NEXT_PUBLIC_SITE_URL=https://realtyguruji.com

# WhatsApp lead notifications via CallMeBot (free, optional)
# Setup:
#  1. Add +34 644 51 95 23 to WhatsApp contacts (name it "CallMeBot").
#  2. Send the message: I allow callmebot to send me messages
#  3. CallMeBot replies with your API key — paste it below.
# CALLMEBOT_PHONE=919811290102        # owner's number incl. country code, digits only
# CALLMEBOT_APIKEY=your-callmebot-key

# OpenAI (optional — enables the real AI chatbot)
# OPENAI_API_KEY=sk-...
